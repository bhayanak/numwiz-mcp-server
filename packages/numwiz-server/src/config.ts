// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface NumWizConfig {
  // Self-contained server — no external API configuration needed.
  // Reserved for future settings.
}

export function loadConfig(): NumWizConfig {
  return {};
}
